'use strict';

/**
 * Module dependencies
 */
let path = require('path'),
  config = require(path.resolve('./config/config'));

/**
 * Friends module init function.
 */
module.exports = function (app, db) {

};
